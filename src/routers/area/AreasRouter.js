const express = require('express')
const router = express.Router()

/** GET */
// router.get('/search/all/:ccId/:aState', AreasController.getAllCC)
// router.get('/search/all/:aState', AreasController.getAllAreaCC)

// router.get('/search/onemod/:eId', AreasController.getOneEmpMod)

/** POST */
// router.post('/register', AreasController.register)
// router.post('/edit', AreasController.edit)
// router.post('/change/state', AreasController.changeState)

module.exports = router;